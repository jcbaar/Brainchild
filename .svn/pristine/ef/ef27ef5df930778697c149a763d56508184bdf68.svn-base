<html>

<head>
<meta http-equiv="Content-Type"
content="text/html; charset=iso-8859-1">
<title>NMSTATUSUPDATE</title>
</head>

<body bgcolor="#FFFFFF">

<p><font color="#000000" size="4"><strong>NMSTATUSUPDATE</strong></font></p>

<table border="0" cellpadding="0" cellspacing="3" width="100%">
    <tr>
        <td width="100%" bgcolor="#E6E6E6"><pre>typedef struct tagNMSTATUSUPDATE
{
	NMHDR	hdr;
	BOOL	bAnyText;
	BOOL	bCanUndo;
	BOOL	bCanRedo;
	BOOL	bCanCut;
	BOOL	bCanCopy;
	BOOL	bCanPaste;
	BOOL	bCanDelete;
	BOOL	bModified;
	BOOL	bOverwrite;
	BOOL	bReadOnly;
	BOOL	bBookmarks;
	int	nFileMode;
} NMSTATUSUPDATE, FAR* LPNMSTATUSUPDATE;
</pre>
        </td>
    </tr>
</table>

<p>Contains information about a <a href="nmstatusupdate.htm">NMBC_STATUSUPDATE</a> notification message.</p>

<dl>
    <dt><em>hdr</em></dt>
    <dd><strong>NMHDR</strong> structure containing information about this notification message.</dd>
    <dt><em>bAnyText</em></dt>
    <dd>Non-zero if there is text in the control and zero if not.</dd>
    <dt><em>bCanUndo</em></dt>
    <dd>Non-zero if there are changes which can be undone and zero if not.</dd>
    <dt><em>bCanRedo</em></dt>
    <dd>Non-zero if there are changes which can be redone and zero if not.</dd>
    <dt><em>bCanCut</em></dt>
    <dd>Non-zero if there is a text selection which can be cut from the text and zero if not.</dd>
    <dt><em>bCanCopy</em></dt>
    <dd>Non-zero if there is a text selection which can be copied to the clipboard and zero if not.</dd>
    <dt><em>bCanPaste</em></dt>
    <dd>Non-zero if there is text on the clipboard which can be pasted in the control contents
    and zero if not.</dt>
    <dt><em>bCanDelete</em></dd>
    <dd>Non-zero if there is a text selection which can be delete from the text and zero if not.</dd>
    <dt><em>bModified</em></dt>
    <dd>Non-zero if the control contents is modified and zero if it is not.</dd>
    <dt><em>bOverwrite</em></dt>
    <dd>Non-zero if the editing mode is iverwrite and zero when the editing mode is insert.</dd>
    <dt><em>bReadOnly</em></dt>
    <dd>Non-zero if the control contents is read-only and zero if it is not.</dd>
    <dt><em>bBookmarks</em></dt>
    <dd>Non-zero if there are bookmarks set in the control contents and zero if not.</dd>
    <dt><em>nFileMode</em></dt>
    <dd>The current file mode. This can be any of the following modes:<br>
    <dl type="desc">
    	<dt><strong>FMODE_MSDOS</strong></dt>
	<dd>Lines end with CR/LF.</dd>
	<dt><strong>FMODE_UNIX</strong></dt>
	<dd>Lines end with LF.</dd>
	<dt><strong>FMODE_MACINTOSH</strong></dt>
	<dd>Lines end with CR.</dd>
    </dl></dd>
</dl>

</table>
</body>
</html>